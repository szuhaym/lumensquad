# Site settings
title:
email:
description: > # this means to ignore newlines until "baseurl:"
baseurl: "" # the subpath of your site, e.g. /blog/
url: "" # the base hostname & protocol for your site

twitter_username:
github_username:

# Build settings
markdown: kramdown

# add these lines near top or anywhere in _config.yml
languages:
  - en
  - hu
default_lang: en
baseurl: "" # keep as you have it
